spring:
  jpa:
    show-sql: true

app:
  datasource:
    identity:
      url: jdbc:postgresql://localhost:5432/exim_identity
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
