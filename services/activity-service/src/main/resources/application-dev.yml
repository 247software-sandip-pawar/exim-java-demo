# Dev profile: defaults in application.yml point MONGODB_URI at a local mongod.
# Set MONGODB_URI to your Atlas connection string to use Atlas.
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: INFO
